permissions: {}
jobs:
  deploy:
    permissions:
      actions: read
      contents: read
    steps:
      - run: |
          ssh -o BatchMode=yes -o StrictHostKeyChecking=accept-new \
            "inside-deploy@${{ secrets.PRODUCTION_SSH_HOST }}"
