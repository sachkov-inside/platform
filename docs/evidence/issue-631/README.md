# Platform #631: общие названия продуктов

Изменённая реализация: `74081ee692e1905c980375b2a6195c120c6d5d19`, база
`32332bd755e5b73d9c725b9a000f65a7022d4a4b`.

Пять существующих production-owned поверхностей в Storybook проверены при 1440×1024 и
390×844: продукт, закрытый Reader, разовая оплата, покупки в кабинете и список автора.
В `results.json` — наблюдавшиеся подписи, отсутствие горизонтального переполнения и
serious/critical нарушений axe WCAG2/2.1 A/AA. Скриншоты сняты после видимого содержимого;
Agentation скрыт только для автоматического снимка и axe, приложение не изменялось.
Авторские названия предложений сохранены, поэтому конкретное «Руководство “Создание Platform
Inside”» рядом с общим «Продукт» ожидаемо. Авторская fixture сохраняет собственную прежнюю
навигацию и симулирует недоступность закрепа; это не проверка backend закрепа.

Воспроизведение из корня checkout на pinned Node/pnpm:

```bash
pnpm --filter @inside/web exec storybook dev --ci -p 6316
# Во втором терминале; каталог вывода выбирается отдельно от сохранённых доказательств.
node docs/evidence/issue-631/capture.cjs /tmp/platform631-new-evidence
```

Это визуальное доказательство настоящих компонентов с fixture-данными, не реальные продажи,
платёжное подтверждение или production. Route metadata и доступные подписи дополнительно
проверяет штатный Playwright в `pnpm check`; server/BFF/business поведение не менялось.
Первый capture выбрал скрытый desktop navigation label на mobile; итоговый использует видимый
заголовок первого уровня. Исходный `pnpm check` встретил timeout незатронутой проверки загрузки
изображения обложки; focused повтор cover/admin — 18 PASS, итог полного повтора записывается в PR.
