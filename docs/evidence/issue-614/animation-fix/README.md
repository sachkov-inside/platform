# Исправление анимации практикума — 14.09.2026

Продолжение [#616](https://github.com/sachkov-inside/platform/issues/616) по просьбе владельца
после принятия страницы. Изменения находятся в локальном рабочем дереве; merge и deploy не выполнялись.

## Что исправлено

- В сцене проверки пустой пользователь завершает функцию до отправки. Безусловное обращение
  к `user.id` удалено. Показанный тест теперь соответствует показанному коду.
- Зелёные статусы теста и сборки появляются после исправления, затем ревью и принятие.
- Мобильная композиция адаптирована к ширине самого слота: подписи 12 px, технические chips
  11 px, код 11.5 px на 390 px. На самом узком слоте подписи этапов 11 px, код 10.5 px.
  Skills, MCP и все шесть строк логов видны. Проверки расположены над кодом.
- Карточка Главной сохраняет высоту анимации не менее 230 px на узких экранах.
- Сюжет, пять сцен, замедление 1.25 и цикл 24.25 секунды сохранены. Названия сцен и полоски
  прогресса не возвращены. Reduced motion остаётся статичной сценой 5.

## Проверка на настоящих маршрутах

Стенд `http://127.0.0.1:3105`, маршруты `/` и `/guides/working-with-agents`.
Desktop 1440 px, mobile 390 px; дополнительно Главная 320 px.
Существующий сервер не перезапускался, Compose и production не менялись.

Снимки фиксируют конечное состояние каждой сцены: на реальном маршруте включён reduced motion,
затем для инспекции переключён активный DOM-слайд. Это статичная инспекция композиции, а не
доказательство естественных таймингов. Настоящий цикл проверен отдельно наблюдением смены
`data-scene`: 1 → 2 → 3 → 4 → 5 → 1 примерно за 24.25 секунды. До исправления статус теста
остаётся невидимым (`opacity: 0`). Возврат к reduced motion показывает сцену 5.

- `guide-390-scene-1.png` … `guide-390-scene-5.png` — герой практикума.
- `guide-1440-scene-4.png`, `guide-1440-scene-5.png` — desktop.
- `home-390-scene-1.png` … `home-390-scene-5.png` — слот Главной 358 × 230.
- `home-320-scene-1.png` … `home-320-scene-5.png` — узкий экран.
- `measurements.json` — размеры, измерения текста и отдельно наблюдение цикла.

Из DOM извлечён конечный текст функции и выполнен с тестовыми `ok`, `send`, `test`, `expect`:
`notify(null)` возвращает `ok` без отправки; валидный пользователь отправляется ровно один раз.

## Автоматические проверки и пределы

- Storybook анимации и страницы: 2 файла, 16 тестов прошли; добавлены отдельные мобильные
  истории всех пяти сцен. Отдельно прошли 6 Storybook tests Главной.
- `pnpm lint`, `pnpm docs:check`, `git diff --check` прошли.
- Корневой `pnpm check` прошёл документацию, contracts, lint, typecheck, guardrails и unit tests:
  backend 603, web 902 (1 skipped), legal 46, access-capabilities 6.
- Полный `pnpm check` остановился на E2E: второй `next dev` не получил lock уже работающего
  проекта. Существующий стенд сохранён. Следующие build/standalone/Storybook build не запускались
  этой цепочкой. Сквозной прогон всего продукта и готовность к production не заявляются.

Documentation impact: локальные детали принадлежат компоненту/CSS и историям; контракт анимации
уточнён в surface brief. Общая готовность и остальные блокеры остаются в
[передаче практикума](../practicum-handoff-2026-09-14.md).
