# Артефакты руководства — #466

Дата: 2026-09-09. Фиксированная база Platform: `aa36a728e30ad88dad736fce6243d1537d1e2577`.

Снимки сделаны на собранном Storybook (`pnpm build:storybook`) для того же модуля
`features/guide-artifacts`, который импортирует продакшен-страница редактора руководства.
Виджеты и размеры совпадают с проектами Playwright: desktop 1440×1024 и mobile 390×844.

| Файл | Что показывает |
|---|---|
| `artifacts-desktop.png` | Раздел «Артефакты» с файловым и ссылочным артефактом, отметками доступа, версии и переиспользования |
| `artifacts-mobile.png` | Тот же раздел на 390 px: карточки в одну колонку, действия переносятся без обрезки |
| `artifacts-empty-desktop.png` | Пустое состояние раздела |
| `artifacts-referenced-desktop.png` | Отказ удалить артефакт, который ещё используется в других руководствах |
| `artifacts-add-link-mobile.png` | Форма нового артефакта по ссылке на узком экране |
| `guide-editor-desktop.png` | Раздел внутри страницы редактирования руководства, ниже материалов |
| `guide-editor-mobile.png` | То же на 390 px |

Страницы редактора сняты с прокруткой внутреннего контейнера `#authoring-content`, поэтому
раздел артефактов виден целиком.

## Чего эти снимки не показывают

Живой маршрут `/authoring/guides/<id>` не снимался: singleton Compose-стек `inside-platform` на
этой машине принадлежит другой сессии, и правило
[`docs/runbooks/local-development.md`](../../runbooks/local-development.md#parallel-worktrees-and-singleton-ownership)
запрещает его перезапускать. Проверки на реальном PostgreSQL выполнены интеграционными тестами
через Testcontainers.

Снимки не являются доказательством деплоя, приёмки владельцем или визуального GO.
