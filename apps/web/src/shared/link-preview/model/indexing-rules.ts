/**
 * Разделы, которым нечего делать в поиске: личный кабинет, авторская часть, служебные маршруты
 * и возврат из оплаты. Совместимый адрес `/series/<slug>` остаётся открытым: поисковику нужно
 * прочитать его канонический адрес, а не упереться в запрет.
 */
export const CLOSED_SECTIONS = [
  "/_health/",
  "/account",
  "/api/",
  "/auth/",
  "/authoring",
  "/bookmarks",
  "/callback",
  "/communications/visit",
  "/subscription/return",
] as const;

/**
 * Исключения внутри закрытых разделов. Обложка в карточке ссылки живёт по адресу доставки
 * обложек, а он лежит внутри `/api/`: без явного разрешения краулер не заберёт картинку.
 */
export const OPEN_SECTIONS = ["/api/content-covers/"] as const;
